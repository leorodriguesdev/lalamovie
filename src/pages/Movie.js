import React from 'react';
import { SafeAreaView, ScrollView, StyleSheet, View,Text } from 'react-native';

export function Movie() {

    return (
        <ScrollView >

                    <Text>TESTETESTE</Text>

        </ScrollView>
    );
};

const styles = StyleSheet.create({

});
